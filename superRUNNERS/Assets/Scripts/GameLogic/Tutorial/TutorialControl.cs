using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TutorialControl : MonoBehaviour
{
    //[SerializeField] private TutorialRoomControl currentRoom;
    //[SerializeField] TutorialManager 
    public GameEvent nextLevel;

    internal void TutorialComplete()
    {
        //invoke next level event for room control
    }

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
